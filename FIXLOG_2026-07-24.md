# HPG Fixlog — Sofort-Fixes + Welle 1 (2026-07-24)

Basiert auf `AUDIT_SKILL-TEAM_2026-07-24.md`. Alle Fixes sind im Code mit
`AUDIT-FIX <ID>`-Kommentaren markiert und wurden mit 14 nachgestellten
Audit-Szenarien verifiziert (14/14 bestanden, inkl. Regressionschecks).

## Geänderte Dateien

| Datei | Fixes |
|---|---|
| `hpg_core/transition_renderer.py` | **R-01**: Time-Stretch-Rate invertiert → `rate = bpm_a / target_bpm_b` (librosa: rate>1 = schneller). Track B läuft im Preview jetzt wirklich auf dem Tempo von A. |
| `main.py` | **F1**: `hpg_config`-Import auf Modulebene → KI-Checkbox löst keinen NameError mehr aus. **F2**: Rekordbox-Export ergänzt fehlende `.xml`-Endung. |
| `hpg_core/exporters/rekordbox_xml_exporter.py` | **F3**: Partial-Export statt Alles-oder-Nichts — nur noch Abbruch bei 0 exportierbaren Tracks, sonst `status="partial"` mit Fehlerliste. |
| `requirements.txt` | **R1/R2**: `soundfile` + `SQLAlchemy` ergänzt (waren nur transitiv); Constraint-Pins (numpy<2 wegen numba) dokumentiert. |
| `hpg_core/dj_brain.py` | **N1/B7**: Intro-/Outro-Scanner werten nur noch den zusammenhängenden Randblock aus (Fenster-Artefakte können Mix-Punkte nicht mehr kapern). **B4**: Mix-In mit Suchfenster (erste ~40 %) + Positionsterm. **B5**: Mix-Out mit Position als primärem Kriterium. **E3**: `unanalysed`-Sections als Mix-Kandidaten ausgeschlossen. **N5**: Notfall-Fallbacks quantisieren jetzt aufs Phrasengitter. **B1**: `calculate_paired_mix_points` quantisiert die Endwerte IMMER (floor/ceil, downbeat-verankert) — der real gerenderte Übergang liegt jetzt auf der Phrase. **N4**: Guard gegen `duration<=0`. |
| `hpg_core/analysis.py` | **N1/B7 (Ursache)**: `analyze_structure_windows` degradiert Fenster-Artefakt-Labels — `outro` am Head-Fenster-Ende und `intro` am Tail-Fenster-Anfang werden zu `main`. |
| `hpg_core/downbeat.py` | **N10**: `first_downbeat` wird per Median über alle validen Beats der Gewinner-Phase zurückgerechnet statt aus einem der (leisen, jittrigen) ersten 4 Beats gelesen. **N11**: Rand-Normierung des Loudness-Akzents (Convolve-Artefakt verzerrte das Phase-Voting). |

## Verifikation

`verify_fixes.py` (im Projektordner ablegbar) stellt die Audit-Szenarien nach:
- N1: Fenster-Outro bei 300 s → `outro_start` jetzt korrekt 480.0 (vorher 300.0 → Mix-Out bei 34 %)
- B7: Tail-Intro bei 420-460 s → `intro_end` jetzt 0.0 (vorher 460.0)
- B4/B5: Mix-Fenster jetzt 11 %→89 % statt 64 %→71 %
- B1: Paar-Punkte auf Phrasengitter (Phase 0.0 ms, downbeat-verankert)
- N4: duration=0 → Track-Werte unverändert statt erfundener Punkte
- Regression: echte Intros/Outros, Multi-Section-Intros und der Normalfall unverändert korrekt

## WICHTIG: Nächste Schritte auf deinem Rechner

1. **Tests laufen lassen** (`tests/` existiert lokal, war in der Cloud-Kopie nicht enthalten):
   `venv312\Scripts\python -m pytest` — Tests, die die ALTEN Fehlwerte erwarten
   (z. B. Mix-Out-Positionen, Stretch-Rate), müssen an das korrekte Verhalten angepasst werden.
2. **Cache invalidieren**: Die Mix-Punkte im Analyse-Cache stammen aus der fehlerhaften
   Logik. Cache-Version in `caching.py` bumpen (oder Cache-DB löschen), sonst bleiben
   alte falsche Mix-Punkte aktiv.
3. **Hör-Check**: 2-3 Preview-Übergänge rendern — insbesondere ein Paar mit BPM-Differenz
   (z. B. 124 vs. 132): Track B muss jetzt tempo-synchron klingen.

## Noch offen (nächste Wellen, siehe Audit-Bericht)

Welle 2: F01 (BPM-Gate), F02 (Key-Confidence), F05/F06/F12, F03/F04, F08, F10.
Welle 3: PA-01, A-02, C-01/C-02, T1/T2, P-01/P-02, A-01, RB-01→A-03→C2.
Welle 4: Renderer-Qualität (R-02/03/04, C3 Equal-Power, R-06/07, C4/C5).

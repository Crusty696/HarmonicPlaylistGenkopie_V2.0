# HPG V2.0 - Production Dependencies
# Umgebung: Python 3.12.x (mind. 3.12.1, NICHT 3.13+ - numba unterstuetzt das nicht).
# Die Pins spiegeln die getestete venv312-Umgebung. Vor jeder Aenderung:
# `pip list` gegen diese Datei halten und die volle Suite fahren.

# Audio-Analyse (librosa-Stack - die vier Pins haengen voneinander ab)
librosa==0.11.0
numpy==1.26.4      # <2.0 erzwungen durch numba 0.59.1 (NICHT einzeln anheben)
numba==0.59.1      # Constraint-Pin fuer librosa (nicht direkt importiert)
llvmlite==0.42.0   # Constraint-Pin fuer numba (nicht direkt importiert)

# Audio I/O
soundfile==0.14.0  # Direkt importiert in main.py, transition_renderer.py, analysis.py
mutagen==1.47.0

# Datenbank (Rekordbox-Import)
SQLAlchemy==2.0.51 # Direkt importiert in rekordbox_importer.py (joinedload)
pyrekordbox==0.4.4 # Optional zur Laufzeit: Rekordbox-Datenbank-Import

# Signal-Processing
scipy==1.13.1      # Required for transition_renderer
pedalboard==0.9.24 # Transition-Effekte & Dynamik
pyloudnorm==0.2.0  # EBU R128 / ITU-R BS.1770 Loudness (LUFS)

# GUI & HTTP
PyQt6==6.10.2
requests==2.34.2   # HTTP-Client fuer ai_engine/ai_launcher

# Test-Toolchain - von pytest.ini vorausgesetzt (-n auto, --cov, qtbot)
pytest==9.1.1
pytest-cov==7.1.0
pytest-xdist==3.8.0
pytest-qt==4.5.0

# Reproduzierbarer Frozen-Build
pyinstaller==6.21.0
